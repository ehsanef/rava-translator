# به‌روزرسانی راوا به نسخهٔ ۱.۲.۰

## اگر از پوشهٔ آمادهٔ همین دستگاه نصب کرده‌ای

فایل‌های پوشهٔ `outputs/Rava` به‌روز شده‌اند. فقط این کارها را انجام بده:

1. صفحهٔ `chrome://extensions` را باز کن.
2. در کارت Rava روی آیکون **Reload / بازخوانی** بزن. نسخه باید **1.2.0** باشد.
3. تب سایتی را که می‌خواهی ترجمه کنی **Refresh** کن. اسکریپت نسخهٔ قبلی در تب باز به‌صورت خودکار جایگزین نمی‌شود.
4. یک جمله را انتخاب کن؛ آیکون **RAVA** باید خودکار کنارش ظاهر شود. روی همان آیکون بزن؛ ترجمه و دکمهٔ **کپی ترجمه** داخل همان صفحه باز می‌شوند.

## اگر ZIP قبلی را جای دیگری Extract کرده‌ای

بستهٔ `Rava-v1.2.0.zip` را Extract کن و محتویات پوشهٔ Rava را در همان پوشهٔ نصب قبلی جایگزین کن. سپس مراحل Reload و Refresh بالا را انجام بده. برای حفظ کلید و ذخیره‌ها، افزونه را Remove نکن.

مدل پیش‌فرض نسخهٔ ۱.۲.۰ `gemini-3.7-flash` است و عبارت «ساخته‌شده توسط Ehsanef» در پنجرهٔ اصلی، تنظیمات و پاپ‌آپ داخل صفحه نمایش داده می‌شود.

## اتصال و سرعت

1. تنظیمات راوا را باز کن. اگر کلید قبلاً دائمی ذخیره شده، لازم نیست دوباره آن را وارد کنی. کلید موقت هنگام Reload پاک می‌شود.
2. برای ترجمهٔ سریع، روی **انتخاب مدل پیشنهادی برای سرعت** بزن. پیشنهاد اولیه `gemini-2.5-flash-lite` است؛ فهرست واقعی مدل‌های حساب در تست اتصال بررسی می‌شود.
3. روی **تست واقعی ترجمه و ذخیرهٔ کلید** بزن. این بار صرفاً فهرست مدل‌ها بررسی نمی‌شود: جملهٔ «Hello! How are you?» با همان مسیر ترجمهٔ اصلی ترجمه می‌شود. یک درخواست API مصرف می‌شود.
4. فقط پس از موفقیت ترجمه، کلید و تنظیمات طبق گزینهٔ نگهداری کلید ذخیره می‌شوند. زمان تست و متن ترجمه نمایش داده می‌شود.

اگر ترجمه خطا بدهد، خطای سهمیه، مدل، دسترسی، سرویس، پاسخ نامعتبر و قطع اتصال از هم تفکیک شده‌اند؛ کد HTTP و وضعیت گوگل، در صورت وجود، کنار پیام نمایش داده می‌شوند. دریافت موفق فهرست مدل‌ها به‌تنهایی موفقیت ترجمه محسوب نمی‌شود.

## تغییرات این نسخه

- آیکون کوچک و مشخص RAVA پس از انتخاب متن با ماوس یا کیبورد؛ با پشتیبانی بهتر از سایت‌هایی که رویدادهای ماوس را متوقف می‌کنند.
- ترجمه داخل همان صفحه، همراه با کپی، تغییر لحن و توقف درخواست. میان‌بر و منوی راست‌کلیک دیگر به صفحهٔ تنظیمات هدایت نمی‌کنند.
- درخواست استاندارد `generateContent` به جای وابستگی به SSE؛ پاسخ کامل بدون `finishReason` دیگر به‌اشتباه «قطعی اینترنت» محسوب نمی‌شود.
- مسیر مشترک برای تست اتصال و ترجمهٔ اصلی، همراه با خطاهای دقیق‌تر.
- مدل پیش‌فرض `gemini-3.7-flash`؛ انتخاب Flash-Lite برای سرعت به‌صورت اختیاری در تنظیمات در دسترس است.
- نمایش عبارت «ساخته‌شده توسط Ehsanef» در پنجرهٔ اصلی، تنظیمات و پاپ‌آپ ترجمه.
- نمایش سریع وضعیت انتظار، بدون تأخیر مصنوعی برای تایپ‌کردن نتیجه.

۱۷ تست منطق/API و ۳۱ بررسی مرورگری با پاسخ‌های کنترل‌شده پاس شده‌اند. نمایش آیکون، کلیک واقعی، کپی و Paste در Chrome واقعی بررسی شدند. اتصال حساب و سرعت واقعی مدل با کلید شما هنوز آزمایش نشده است؛ تست جدید داخل تنظیمات این بررسی را روی دستگاه شما انجام می‌دهد.
